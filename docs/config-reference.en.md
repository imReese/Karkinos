# Configuration Reference

> Status: translation stub.

The maintained configuration reference is [config-reference.zh.md](config-reference.zh.md).

The executable configuration contract is the typed configuration code plus `config.example.json`; this translation does not define a second schema.
