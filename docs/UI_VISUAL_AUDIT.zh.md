# 全路由视觉审计

审计日期：2026-07-27

本文记录 Karkinos 完成各路由整页重构后的稳定视觉状态。`design.md` 继续是 UI
唯一 source of truth；本文只是验收快照和优先级边界，不是第二套设计系统。

## 范围与方法

审计使用正在运行的应用和本地持久化开发数据库，不注入 mock 财务数据，不联系行情
provider，也不触发券商、OMS、资本或订单提交权限。

整页审计覆盖：

- 公开首页 `/`；
- 总览、组合、账本、风控、Account Truth、决策、运营、行情、执行、回测、AI 研究和设置；
- 代表性持仓详情 `/portfolio/603659`。

视觉基线覆盖 1440 x 900、1280 x 800、1024 x 768、834 x 1112、768 x 1024 和
390 x 844；Latte 与 Mocha 均在桌面、平板和移动检查中实际运行。每个页面都等待
canonical GET 进入稳定证据状态后再判断视觉层级。审计项目包括：

1. 首屏主角与五秒理解能力；
2. 页面构图和任务顺序；
3. 图表权重以及图表与证据的关系；
4. 列表—详情关系；
5. 桌面、平板和移动端重排；
6. 单一 H1、local overflow、默认披露状态和权限边界；
7. 缺失、过期、降级、阻断和已对账状态语言。

## 第二轮收口

第二轮针对首次全路由审计后暴露的构图与状态语义漂移完成以下修正：

- Overview、Risk 与 Market 把瞬态 loading 和 empty、missing、stale、error 明确分开；
  持久化 GET 变慢时不再误导用户认为证据不存在。
- Activity 以不可变账本历史为主，录入工具只在需要时展开；Operations 以优先队列为主，
  在稳定详情抽屉中查看证据，技术身份默认下钻。
- 持仓详情在持久化价格序列缺失时继续显示 canonical 持仓事实；768-1024px 下证据栏
  堆叠在摘要之后，到 1280px 才恢复桌面分栏。
- 公开首页用紧凑产品路径替换巨大、静态的概念卡；首屏明确 `/` 到 `/overview` 的
  公开页—私有工作台转换、证据到人工复核的路径，以及默认“仅查看与复核”的权限，
  不虚构账户、收益、订单或成交数据。

第二轮没有引入 provider 读路径、UI 财务重算、券商能力或新的平行设计系统。

## 参考矩阵

以下产品提供交互原则，不提供可直接复制的主题。

| 参考                                                                                      | 保留原则                                            | Karkinos 的落地                                                             | 明确不做                                        |
| ----------------------------------------------------------------------------------------- | --------------------------------------------------- | --------------------------------------------------------------------------- | ----------------------------------------------- |
| [Apple Stocks](https://support.apple.com/guide/stocks/welcome/mac)                        | 自选列表—详情构图、强标的身份、由图表拥有详情主画布 | Market 和持仓详情把当前标的、持久化行情状态、时间区间与图表放进同一阅读路径 | 复制 Apple 外壳，或为了安静而隐藏 provenance    |
| [Google Finance](https://www.google.com/finance/)                                         | 搜索优先、紧凑市场摘要、可比较列表                  | 全局搜索保持稳定，持仓和行情列表使用紧凑可比较行                            | 混淆大盘表现与个人组合贡献                      |
| [Cursor](https://cursor.com/) 与 [Linear](https://linear.app/)                            | 单一主任务、安静 chrome、显式状态、降低噪音         | Decision、Risk、Operations 和 Trading 先显示当前人工任务或阻断              | 把每个子系统都做成同等权重的 dashboard 卡片     |
| [Stripe](https://stripe.com/) 与 [Vercel](https://vercel.com/)                            | 编辑式公开首页、克制的产品证明和 CTA                | 公开首页先解释 Karkinos，再进入工作台                                       | 把营销页的大留白、渐变和巨大 Hero 带进业务路由  |
| [Bloomberg Terminal](https://www.bloomberg.com/professional/products/bloomberg-terminal/) | 稳定信息架构与专业领域密度                          | 桌面导航、表格和核心证据保持稳定位置与高信息产出                            | 复刻终端杂讯、专有约定或无层级密度              |
| [Koyfin](https://www.koyfin.com/)                                                         | 上下文丰富的 dashboard、强图表和连续工作区          | 图表只在能回答页面核心问题时占据主画布，并保留周围证据                      | 允许 canonical 概念在自定义 widget 中跨页面漂移 |
| [TradingView](https://www.tradingview.com/)                                               | 图表优先的标的研究、本地时间控制和清晰市场上下文    | Market、持仓详情与 Backtest 为图表提供足够空间和局部控制                    | 直连券商、视觉化催促交易或引入第二套图表系统    |

组合目标不是拼贴。公开页面借鉴 Cursor、Linear、Stripe 和 Vercel 的编辑式清晰度；
业务页面借鉴 Apple、Google 的 master-detail 纪律，以及 Bloomberg、Koyfin、TradingView
的图表与数据密度。Karkinos 保留自己的证据优先状态语言、Catppuccin 双主题、人工授权
边界和 canonical persisted projections。

## 路由结果

| 路由                 | 审计状态   | 首屏主角                   | 结论与剩余观察                                                                                          |
| -------------------- | ---------- | -------------------------- | ------------------------------------------------------------------------------------------------------- |
| `/`                  | 通过，标杆 | 品牌主张与产品路径         | 紧凑首屏把主张、结构化证据路径、公开/私有路由身份和默认“仅查看与复核”权限放在一起，不使用虚构财务数据。 |
| `/overview`          | 通过       | 账户事实、持仓与安全下一步 | 账户指标和证据领先；loading 不冒充证据缺失，持仓与行动优先级清楚，没有 routine card nesting。           |
| `/portfolio`         | 通过       | 当前持仓主表               | 持仓保持在次级分析之前；已实现历史、清仓资产和账本下钻继续保留。                                        |
| `/portfolio/$symbol` | 通过，标杆 | 标的身份与价格画布         | 把 Apple Stocks 的主从纪律转为持久化证据视图；价格序列缺失时仍保留 canonical 持仓事实，平板端顺序堆叠。 |
| `/activity`          | 通过       | 不可变活动时间线           | 历史优先，受控录入工具按需打开，不与审计路径竞争。                                                      |
| `/risk`              | 通过，标杆 | 当前阻断与解除条件         | 异常、指标、阈值与 kill switch 边界相互区分；瞬态 loading 不再标记为证据缺失。                          |
| `/account-truth`     | 通过       | 当前对账报告               | 稳定后的报告、分数、异常和匹配明细一致；瞬态 loading 正常消失，不作为永久缺陷处理。                     |
| `/decision`          | 通过，标杆 | 下一决策与 gate matrix     | 账户事实、研究、风控、paper/shadow 与人工复核形成单一阶段化决策。                                       |
| `/operations`        | 通过       | 优先证据队列               | 可行动异常进入稳定证据抽屉；不可变历史更安静，技术指纹默认下钻。                                        |
| `/market`            | 通过，标杆 | 标的列表与当前图表         | 列表—图表关系跨端稳定；持久化行情变慢时保持 loading，不误报缺失，个人证据不冒充大盘热力图。             |
| `/trading`           | 通过       | 人工确认队列               | 空待确认状态真实，券商权限继续关闭。只有真实使用证据证明空状态阻碍审计任务时，才考虑整页级压缩。        |
| `/backtest`          | 通过，标杆 | 持久化报告与净值/回撤证据  | 有结果时，已保存证据、成本后指标、图表、可复现身份和推广门禁高于设置控件。                              |
| `/ai-research`       | 通过       | 冻结证据的人工研究任务     | 空复核状态保持真实，不用虚构研究填充空间；只有任务表单确为主任务时才保留大输入画布。                    |
| `/settings`          | 通过       | 持久化配置                 | 页面较长但采用渐进披露；刷新和权限相关操作继续与配置事实分离。                                          |

## 跨端结果

| 基线              | 稳定结果                                                                        |
| ----------------- | ------------------------------------------------------------------------------- |
| 1440 x 900，Mocha | 所有路由均保持单一可见 H1、文档与 shell 零横向溢出，以及稳定桌面信息层级。      |
| 1280 x 800，Latte | 导航、命令栏、内容画布、图表、表格和受控操作区无首屏碰撞。                      |
| 1024/768，混合    | Shell 使用平板画布和底部导航；持仓摘要与证据顺序堆叠，文档无横向溢出。          |
| 834 x 1112，Latte | Master-detail 页面主动再平衡，图表没有缩成装饰缩略图；任务页面按阅读顺序堆叠。  |
| 390 x 844，Mocha  | 移动导航不覆盖当前任务；有意横向滚动被限制在 local rail；低优先级披露默认关闭。 |

主题对称、移动触控目标、reduced motion 和默认披露状态继续由确定性前端与 Playwright
contract 覆盖。页面初始出现 loading 或 missing 时，必须等 canonical GET 稳定后再判断；最终
缺失或过期证据仍必须显式展示并 fail closed。

## 验证证据

验证使用 Node.js 24.18.0 和 no-live 本地应用服务器：

- `web/node_modules/.bin/prettier --check docs/UI_VISUAL_AUDIT.md docs/UI_VISUAL_AUDIT.zh.md`
  通过；
- `cd web && npm test` 通过 66 个文件、562 个测试；
- `cd web && npm run build` 完成 TypeScript 与 Vite production build；
- `cd web && npm run test:e2e` 在 5.0 分钟内通过全部 18 个 Playwright 测试；
- 内置浏览器等待 14 个被审路由进入稳定状态，并在抽样宽度下确认 document 与 app shell
  均无横向溢出。

首次在沙箱内启动 Playwright 时无法绑定 localhost 测试端口；随后使用受限本地端口权限运行
同一 no-live 命令。一次最终全量执行中，Trading 移动端 DOM 已满足契约，但耗尽通用 30 秒
测试预算；该用例隔离重跑在 0.662 秒内通过。只把测试预算与现有跨端用例对齐后，全量 18 项
连续通过，未放宽断言，也未改变生产行为。所有运行都没有启用 provider 或券商连接。

## 优先级结论

本次审计没有发现 P0 或 P1 整页视觉缺陷。

以下只是观察触发器，不构成已授权修改：

- Trading：只有使用证据表明长期 0 待确认持续阻碍审计任务时，才重新评估空队列；一旦存在
  pending confirmation，队列必须继续是首屏主角。
- AI Research：只有出现持久化复核任务后，才重新评估桌面空状态平衡；禁止用概念研究或 mock
  内容填充空间。
- Settings：只有 operator 测试证明渐进披露仍不足时，才考虑拆分导航；禁止为了缩短页面再造
  routine panels。

按钮大小、边框、时间格式、badge 打磨等局部细节继续低于当前优先级。只有出现首屏层级失败、
阻断被隐藏、图表—列表关系失败、跨端不一致、权限歧义、canonical 状态冲突、可访问性回归或
overflow 时，才恢复 UI 修改。

## 完整性与权限结果

审计没有发现需要修改金融契约的 UI 路径。所有被审页面继续消费 canonical persisted
projections；读路径不联系 provider、不写库；UI 不重算价格、成本、P&L、风险、配置或权限；
缺失和过期证据继续显式展示。策略与 AI 不获得券商权限，Trading 继续默认人工确认，且无默认
submit、cancel、自动恢复或资本扩容能力。
