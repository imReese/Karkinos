# Frozen Broker Order-Lifecycle Ingestion Reference

> Status: frozen / later-stage reference.

This path remains for compatibility and acceptance evidence. It does not define the current roadmap.

Use [PLAN.md](PLAN.md) and [ARCHITECTURE.md](ARCHITECTURE.md); current code/tests own lifecycle semantics.

Future live ingestion must conform to the unified canonical Order/Fill lifecycle rather than revive a parallel state model.
